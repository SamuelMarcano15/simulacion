// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir configuración de Next.js en el futuro si lo necesitas.
  // Por ejemplo: reactStrictMode: true,
};

export default nextConfig;